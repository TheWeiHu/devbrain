{
  "_note": "Your personal classifier overrides. This file OVERLAYS the built-in default: set only the keys you want to change — anything you leave out keeps tracking the shipped default and improves on upgrade. Available keys: wrapper_strip_regex, command_extract_regex, system_prefixes, system_head_contains, title_gen_prefixes, nightshift_prefixes, command_prefix, autonomous_cwd_regex, autonomous_worktree_regex, payload_voice_regex, repeat_signature_len, repeat_long_words, repeat_min_copies_short, repeat_min_copies_long, payload_min_words, payload_cross_project_min."
}
